config:                          
    #browser can be -> firefox, ie, chrome, safari
    browser: firefox
    #fixtures are languages.yml files
    fixtures: test_data
    #true/false = check if there are missing translations on scenario end
    check_translation: true
    #this is a regexp template for validation on page for missing translations
    translation_tag: "@[0-9a-zA-Z]+_[0-9a-zA-Z]+"